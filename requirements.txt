# AI Resume Screening System — Dependencies
# ==========================================
# Core
streamlit>=1.33.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0

# NLP / ML (for production BERT fine-tuning)
torch>=2.1.0
transformers>=4.38.0
scikit-learn>=1.3.0
tqdm>=4.66.0

# File parsing (resume upload)
pymupdf>=1.23.0          # PDF extraction (PyMuPDF / fitz)
pdfminer.six>=20221105   # PDF fallback
python-docx>=1.1.0       # DOCX extraction

# Optional utilities
python-dotenv>=1.0.0
